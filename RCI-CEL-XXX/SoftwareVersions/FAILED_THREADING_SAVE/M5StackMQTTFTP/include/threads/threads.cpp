#include "M5StackMQTTFTP.h"

using namespace ESPressio::Threads;

/*
class DisplayManagementThread : public Thread {
  protected:
    void OnInitialization() override {
      // Anything we need to do here prior to the Thread's Loop starting
    }

    void OnLoop() override {
      // Whatever we want to do within the Loop
      delay(10);
    }
};
*/

/*
class InternalManagementThread : public Thread {
  protected:
    void OnInitialization() override {
      // Anything we need to do here prior to the Thread's Loop starting
    }

    void OnLoop() override {
      // Whatever we want to do within the Loop
      delay(10);
    }
};
*/